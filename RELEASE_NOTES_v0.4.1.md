# InsightFlow Pro v0.4.1 Release Notes

## 定位

v0.4.1 是一次 Production & Portfolio Hardening，不再扩张业务页面，而是修复可复现性、状态持久化、只读部署、缓存、测试和交付工程问题。

## 主要改进

- Bootstrap实验随机种子改为SHA-256稳定哈希，跨进程结果可复现；
- 修复 `elasticity_override=0.0` 被错误替换为默认弹性的边界问题；
- 新增SQLite `action_register`，使用稳定问题指纹去重并保存负责人、截止日期、状态、完成时间和备注；
- 新增真正的只读运行策略，写操作在后端和界面双重阻断；
- HTML与Word报告支持内存生成，无需在公开服务器写入本地文件；
- SQLite查询加入数据库/WAL版本缓存，返回副本避免缓存对象被调用方修改；
- 项目改为标准可安装Python包，新增 `insightflow` CLI；
- Docker改用非root用户、健康检查、只读根文件系统、`no-new-privileges`、能力降权与资源限制；
- CI增加覆盖率门槛、安全扫描、依赖审计和Docker构建；
- 自动化测试从13项增加到19项，核心后端覆盖率达到84%。

## 验证

- 标准化交易记录：107,394；
- 七表仓库记录：51,137；
- 测试：19 passed；
- 核心后端覆盖率：84%；
- KPI查询基准：约0.49秒（首次完整查询，107,394条明细）；
- HTML与Word报告重新生成成功；
- Power BI导出包重新生成成功。

## 已知边界

- 当前环境未安装Streamlit，因此没有在本次容器内启动浏览器和执行AppTest；
- 当前环境没有Docker守护进程，因此Dockerfile已完成静态与命令级检查，但未在本容器内实际构建镜像；
- 原生 `.pbix` 仍需在Power BI Desktop中根据现有模型包建立；
- 依赖采用范围约束并由CI执行安全审计，尚未提交由目标部署环境生成的完整锁文件。
